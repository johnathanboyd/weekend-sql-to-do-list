-- db name tasks ---

CREATE TABLE "tasks"(
    "id" serial primary key,
    "title" varchar(50),
    "description" varchar(250)
);

INSERT INTO "tasks" ( title, description ) VAlUES ();
SELECT * from "tasks";